name: Permissions Probe

on:
  push:
    branches:
      - dincer552-fix-github-updates
  workflow_dispatch:

permissions:
  contents: write

jobs:
  probe:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v4
      - shell: pwsh
        run: Write-Host "contents: write probe OK"
